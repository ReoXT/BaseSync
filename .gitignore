node_modules/
.wasp/out/
.wasp/.waspchecksums
.wasp/.wasproot

# Keep .wasp/build for Railway deployment
# .wasp/build/

# Keep folders for Railway deployment (copied from .wasp/build)
# These are required by the Dockerfile
# db/
# sdk/
# server/

# Client deployment folder (temporary build artifact)
client-dist/

# Environment files with credentials (NEVER commit these!)
.env.server
.env.development
.env.production
.env
.env.local

# Deployment notes with sensitive credentials
DEPLOYMENT.md
