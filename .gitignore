node_modules/
.wasp/out/
.wasp/.waspchecksums
.wasp/.wasproot

# Keep .wasp/build for Railway deployment
# .wasp/build/

# Environment files with credentials (NEVER commit these!)
.env.server
.env.development
.env.production
.env
.env.local

# Deployment notes with sensitive credentials
DEPLOYMENT.md
